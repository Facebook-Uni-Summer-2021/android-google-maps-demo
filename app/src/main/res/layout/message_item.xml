<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout android:id="@+id/layout_root"
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="10dp">

    <!--Represents the input window-->
    <EditText
        android:id="@+id/etTitle"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@+id/etSnippet"
        android:paddingTop="20dp"
        android:hint="Title">
    </EditText>

    <EditText
        android:id="@+id/etSnippet"
        android:layout_below="@id/etTitle"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Snippet">
    </EditText>

    <TextView
        android:id="@+id/tvIconHint"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Icon"
        android:layout_below="@+id/etSnippet"
        android:layout_alignParentLeft="true"
        android:textSize="20sp"
        android:layout_marginTop="12dp"
        android:layout_marginEnd="10dp">
    </TextView>

    <ImageView
        android:id="@+id/ivIcon"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/etSnippet"
        android:layout_toEndOf="@+id/tvIconHint"
        android:background="@android:drawable/ic_menu_directions"
        android:onClick="onClick">
    </ImageView>
</RelativeLayout>